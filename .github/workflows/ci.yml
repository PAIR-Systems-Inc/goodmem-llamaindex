name: Tests
on:
  push:
    branches: [main]
  pull_request:
  workflow_call:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.13']
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python }}
      - run: uv venv
      - run: uv pip install -e '.[dev]'
      - run: uv run --no-project ruff check llama_index tests
      - run: uv run --no-project ruff format --check llama_index tests
      - run: uv run --no-project pytest -q
      - run: uv build
